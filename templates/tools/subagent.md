Spawn a background subagent to handle a complex or long-running task.
Actions: 'spawn' starts a task and returns a task_id immediately; 'result' retrieves output for a task_id (poll until DONE); 'list' shows all tasks and their status.
